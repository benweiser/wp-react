describe("Posts Container", () => {
  it("should receive not request api data if it's already been received", () => {
    return undefined;
  });

  it("should request api data if it doesn't exist", () => {
    return undefined;
  });

  it("should render a list of Posts if posts exists", () => {
    return undefined;
  });

  it("should deliver an error message if posts fail to load", () => {
    return undefined;
  });

  it("should return a PostsReducer from mapStateToProps", () => {
    return undefined;
  });

  it("should return a dispatch of requestAPIData()", () => {
    return undefined;
  });
});
